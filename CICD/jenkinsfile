pipeline {
    agent any
    tools {
        nodejs "node"
    }
        stages {
        
           stage('get repo'){
            steps {
            sh 'git clone https://github.com/edgaregonzalez/app-devops-bootcamp.git'
                }
            }
 
        stage('install') {
            steps {
              dir('app-devops-bootcamp/api-store') {
                sh 'npm install'
                }
            }
        }
        
          stage('Build') {
            steps {
              dir('app-devops-bootcamp/api-store') {
                sh 'npm run build'
                }
            }
        }
    
        stage('Deploy') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'DOCKER_ID', passwordVariable: 'PASSWORD', usernameVariable: 'USERNAME')]) {
                sh 'docker login --username ${USERNAME} --password ${PASSWRD}'
                sh 'docker build -t  ${USERNAME}/api-store:lts'
                sh 'docker push  ${USERNAME}/api-store:lts'
            }
            }
        }
    }
}
